#include <stdio.h>
/**
* main - A program that prints all single digit numbers
* Return: always returns 0 (success)
*/
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
		return (0);
}

